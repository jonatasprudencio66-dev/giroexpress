/* eslint-disable */
const axios = require('axios');

const api = axios.create({
 baseURL: "https://strong-lies-help.loca.lt",
  headers: {
    "bypass-tunnel-reminder": "true"
  }
});

module.exports = api;