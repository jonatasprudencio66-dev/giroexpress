/* eslint-disable */
const axios = require('axios');

const api = axios.create({
  baseURL: "https://lucky-words-lick.loca.lt",
  headers: {
    "bypass-tunnel-reminder": "true"
  }
});

module.exports = api;